// var → function scoped
// It works only inside the function where it is declared

// let → block scoped
// It works only inside the block { }

// const → block scoped
// Its value cannot be changed after declaration
